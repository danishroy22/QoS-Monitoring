# Scripts

## Available now

```bash
# From repository root
python scripts/run_simulator.py --mode batch --samples 120
python scripts/run_simulator.py --mode live --ticks 20 --interval 2
```

## Planned

- Starting the backend
- Creating and seeding the database
- Training anomaly detection models
- Running evaluation experiments
